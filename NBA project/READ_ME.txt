The code runs on a server cant be run local

1.open a cloud server and drop the files in.

2.run the commands to start :
   postgres DB
   Airflow

3.run the Dag in in airflow
4.the Data should load in Postgres
